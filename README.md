# CustomConstraint-in-Routing-of-ASP.NET-Core
This repoistary shows how we define custom constraint in parameterized routing of asp.net core. Also it shows that how regex and regular Expressions are working in ASP.NET
